from setuptools import setup

setup(
    name='OE-Docking-Base',
    version='0.1',
    py_modules=[
    ],
    install_requires=[
        'Click',
    ],
    entry_points='''
        [console_scripts]
    ''',
)
